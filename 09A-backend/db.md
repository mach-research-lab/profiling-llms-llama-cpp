

**Steps to use the DB:**

**1. Install docker**
````bash
./install.sh
````
**2. Install psycopg, binary version**

````bash
pip install psycopg[binary]
````


**3. Run the following to start the DB and run the main.py**
````bash
./dev.sh
````





